In this example, when a user submits a message through the form,
the server receives the message, simulates some processing time,
and then emits a notification to all connected clients.
The client-side script listens for notifications and updates the UI accordingly.